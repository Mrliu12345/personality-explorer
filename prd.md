# 人格测试 App · 产品需求文档（PRD）

> 文档版本：v1.0
> 编写日期：2026-09-21
> 文档状态：已确认，待开发
> 说明：本文档为后续开发的唯一需求依据。需求变更时需更新本文档并升级版本号；「第一版不做」清单中的内容未经评审不得重新进入默认范围。

---

## 1. 产品背景与目标

- **背景**：计划开发一款人格测试 App，帮助使用者了解自身人格特质，从而提升自我认知与个人发展。
- **产品目标**：
  1. 提供科学、易读、可信赖的人格测评与结果解读；
  2. 支持多人在同一台电脑上各自独立使用（多档案机制）；
  3. 纯本地运行，数据完全由使用者掌控，无任何联网依赖；
  4. 第一版完成「测 → 看报告 → 给建议 → 导出分享」的核心闭环。

---

## 2. 目标用户与核心需求

### 2.1 目标用户

- 主要使用者：App 开发者本人及其身边朋友/同事（已在需求确认阶段确定）。
- 使用方式：同一台电脑的浏览器，多人轮流使用。

### 2.2 核心需求

1. **即开即用**：无需注册、登录，打开浏览器即可开始；
2. **数据隔离**：每个人有独立档案，各自测试记录互不干扰；
3. **结果可信**：使用科学测评模型，报告内容经得起推敲；
4. **看得懂**：结果解读用通俗中文，非心理学背景也能读懂；
5. **有指导性**：基于结果给出自我认知与个人发展方向的建议；
6. **可分享**：单份报告可导出为图片或 HTML 文件发给他人；
7. **不丢数据**：刷新、关闭浏览器、重启电脑后，所有数据仍保留。

---

## 3. 产品边界与技术约束（硬性约束）

| 项目 | 约束 |
|---|---|
| 运行形态 | 单页 Web 应用（SPA），仅在本机浏览器运行 |
| 登录/注册 | 不做 |
| 联网 | 不做（无任何网络请求） |
| 云数据库/后端 | 不做 |
| 部署上线 | 不做 |
| 手机端/App 上架 | 不做 |
| 数据存储 | 浏览器本地 IndexedDB（结构化存储：档案、逐题作答、报告、进度） |
| 数据持久性 | 刷新、关闭、重启均不丢失 |
| 语言 | 全中文界面 |

---

## 4. 测评模型（v1 选用）

| 量表 | 测量维度 | 选用理由 |
|---|---|---|
| **大五人格**（核心量表） | 开放性、尽责性、外向性、宜人性、情绪稳定性 | 学术界公认度最高的特质模型，结构清晰、跨文化信效度好，适合作为主测评 |
| **心理韧性** | 韧性相关维度 | 补充应对压力与挫折的视角，贴合「个人发展」目标 |
| **核心自我评价** | 核心自我评价水平 | 与自尊、自我效能相关，补充「自我认知」的深度 |

- **题目来源**：优先采用公开可免费使用的学术量表（如 IPIP 国际人格项目库公共量表），规避版权风险；所有题目本地内置，不依赖网络加载。
- **量表形态**：全中文；5 点同意度量表（非常不同意 → 非常同意）；每题一句题干。
- **题量建议**：大五人格 40–60 题（完整版）；心理韧性、核心自我评价各 20–40 题；最终以选定的实际量表为准。
- **作答说明**：每个量表开始前展示「测什么、题数、预计用时」。

---

## 5. 功能模块设计

### 5.1 多档案系统

- 以「档案」替代账号：每个使用者一个档案，档案名为 1–20 字，允许重名；
- 每个档案独立保存其全部数据（测试记录、答题进度、报告），互不干扰；
- 支持操作：**新建、切换、重命名、删除**；
- 删除档案为破坏性操作：二次确认弹窗，明确提示「该档案含 N 份报告，删除后不可恢复」，确认后才执行，执行后回到档案选择页。

### 5.2 问卷作答

- 逐题作答，一屏一题，减少认知负担；
- 顶部进度条：显示「第 n/N 题」与完成百分比；
- 支持「上一题」回改；未作答时「下一题」按钮置灰；
- **每题作答立即写入本地存储**；中途退出自动保存断点，下次进入该档案时提示「有 1 份未完成测试」，点击可回到退出时的题目继续作答；
- 最后一题完成后点击「完成并查看结果」提交，报告由本地计算生成，无网络依赖。

### 5.3 结果分析

单份量表报告包含：

- **总览**：雷达图展示各维度得分；
- **维度解读**：每个维度一张解读卡，包含得分条、高分/低分含义、测试者所处位置解读；
- **综合小结**：基于维度组合生成的总结性文字（非单一模板）；
- **复测标注**：同一量表第 N 次测试自动标注，并提示与上次结果的变化。

多量表综合画像：已完成的不同量表结果并列呈现（v1 为基础版）。

### 5.4 个性化建议

- 建议按维度得分映射生成，保证与测试结果一致；
- 分为三类：
  - **自我认知类**：如何接纳与扬长；
  - **个人发展类**：适合的工作方式与成长方向；
  - **人际类**：与不同性格的人相处的方式。

### 5.5 历史与对比

- 每个档案可查看全部历史报告，按时间倒序排列（量表名、日期、第 N 次）；
- 点击任意一份报告可查看详情（复用报告视图）；
- **对比模式**：勾选 ≥2 份同一量表的报告，生成叠加雷达图并标注各维度升降（↑/↓ 与变化幅度）；少于 2 份时对比按钮置灰。

### 5.6 结果导出

- 单份报告导出为 **PNG 图片**；
- 单份报告导出为**独立 HTML 文件**（可离线打开查看完整报告）；
- 全部数据可导出为 **JSON 备份文件**；
- 导出失败时提示用户重试。

### 5.7 数据与隐私

- 所有数据仅存本机浏览器，无任何网络请求、无统计追踪；
- 界面常驻提示「所有数据仅保存在这台电脑的浏览器中」；
- 设置页提供数据管理：隐私说明、导出备份、删除当前档案、清除全部数据；
- 清除全部数据为破坏性操作，二次确认后执行。

---

## 6. 页面结构设计（7 个视图，SPA 内切换）

```
首页 · 档案选择
└── 档案首页 · 我的空间
    ├── 量表介绍页（×3：大五人格 / 心理韧性 / 核心自我评价）
    │   └── 答题页
    │       └── 结果报告页
    │           └── 导出分享（PNG / 独立 HTML）
    ├── 历史报告页（单份详情 / 多份对比）
    └── 设置与数据页（隐私说明 / 导出备份 / 删除档案）
```

| # | 视图 | 核心职责 | 关键元素 |
|---|---|---|---|
| 1 | **首页 · 档案选择** | 多人入口，用档案替代账号 | 空态引导语（首次使用）；档案卡片列表（档案名、创建时间、最近测试）；「新建档案」按钮 |
| 2 | **档案首页 · 我的空间** | 个人中心与一切操作起点 | 顶部栏（当前档案名、切换档案、设置入口）；测试中心（3 个量表卡片：名称、简介、状态标签「未测/已测/第 N 次」）；最近报告摘要（雷达图缩略 + 一句话结论）；常驻隐私提示 |
| 3 | **量表介绍页** | 测试前知情 | 量表说明（测什么、题数、预计用时、结果用途）；已测过则展示上次结果概要 + 「复测可对比变化」；「开始测试 / 继续上次」按钮 |
| 4 | **答题页** | 核心作答体验 | 返回按钮（保存进度退出）；进度条（第 n/N 题）；一屏一题（题干 + 5 点选项组）；上一题 / 下一题（最后一题为「完成并查看结果」） |
| 5 | **结果报告页** | 价值核心，可导出 | 头部（量表名、档案名、时间、第 N 次测试）；雷达图总览；逐维度解读卡；个性化建议；综合小结；操作区（导出 PNG、导出 HTML、复测、返回）；附「仅作自我了解参考，非心理诊断」提示 |
| 6 | **历史报告页** | 回顾与成长追踪 | 时间倒序报告列表；单份详情；对比模式（勾选 ≥2 份同量表报告 → 叠加雷达图 + 维度升降标注） |
| 7 | **设置与数据页** | 隐私与数据主权 | 隐私说明；数据管理（导出备份、删除当前档案、清除全部数据，均二次确认）；关于（版本、量表来源说明） |

---

## 7. 交互设计

### 7.1 核心流程

**主流程**：打开 App（档案选择）→ 档案首页 → 量表介绍 → 答题（逐题）→ 提交生成报告 → 结果报告 → 导出 / 返回

**分支流程**：

1. **中断恢复**：答题中退出 → 进度自动保存 → 再次进入该档案时提示「有 1 份未完成测试」→ 点击回到断点题目；
2. **复测**：档案首页量表卡片点「复测」→ 跳过介绍直接答题 → 报告自动标注「第 N 次」并提示与上次的变化；
3. **对比**：历史页勾选 ≥2 份同量表报告 → 叠加雷达图 + 维度升降标注；
4. **删除档案**：设置页点删除 → 二次确认（提示含 N 份报告、不可恢复）→ 删除后回到档案选择页。

### 7.2 交互规则表

| 场景 | 交互行为 | 用户反馈 | 异常/边界处理 |
|---|---|---|---|
| 新建档案 | 输入 1–20 字名称（允许重名） | 创建成功，进入档案首页 | 空名称时按钮置灰 |
| 答题跳题 | 点「下一题」 | 新题出现、进度条推进、已答选项保持高亮 | 未作答时「下一题」置灰；「上一题」可回改 |
| 中途退出 | 点返回 | 提示「进度已自动保存」 | 已答数据立即落盘，不丢失 |
| 提交报告 | 完成最后一题点「完成」 | 短暂「正在生成报告」 | 纯本地计算，无网络依赖 |
| 导出 | 点「导出 PNG / HTML」 | 浏览器下载文件 | 失败时提示重试 |
| 结果对比 | 勾选 2–3 份同量表报告 | 叠加图 + 变化标注 | 少于 2 份时对比按钮置灰 |
| 删除/清空 | 设置页操作 | 弹窗二次确认（说明影响与不可恢复） | 确认后才执行，执行后返回档案选择页 |

### 7.3 体验基调

- 柔和中性配色、低刺激视觉（适合自我探索场景）；
- 一屏一题，降低认知负担；
- 支持键盘操作；
- 破坏性操作一律二次确认，常规操作零打扰；
- 全中文、无广告。

---

## 8. 界面设计要求

- 桌面浏览器优先，窗口缩放 / 窄窗口下布局正常（响应式降级）；
- 正文不小于 14px，颜色对比度足够；仅用颜色区分信息时必须辅以文字/形状；
- 报告页阅读舒适，图表与文字并存，不做纯装饰性动效；
- 所有交互有即时反馈（进度、保存、生成中状态）。

---

## 9. 第一版范围

### 9.1 v1 必须完成

1. 3 个测评量表（大五人格、心理韧性、核心自我评价）及本地题库；
2. 问卷作答全流程（进度、回改、断点续答）；
3. 结果报告（雷达图、维度解读、综合小结）；
4. 个性化建议（自我认知 / 个人发展 / 人际三类）；
5. 多档案系统（新建、切换、重命名、删除）；
6. 历史记录与同量表多份对比；
7. 数据持久化（IndexedDB，刷新/关闭/重启不丢）；
8. 报告导出（PNG、独立 HTML）与全量 JSON 备份；
9. 全中文界面 + 隐私提示。

### 9.2 v1 明确不做（未经评审不得进入范围）

- 登录/账号/注册系统；
- 联网功能、云同步、多设备同步、任何服务器部署；
- 手机端 App、应用商店上架；
- 付费、会员、广告；
- 大规模量表库扩充（v1 仅 3 个量表，验证后再扩充）；
- 好友互测、对比排行榜、社区功能；
- 管理后台、复杂统计报表；
- 多语言版本。

---

## 10. 开发验收要点

开发完成后按以下清单核对（对应「完成标准」）：

- [ ] 刷新、关闭浏览器、重启电脑后数据不丢失；
- [ ] 多档案数据互不干扰（A 的测试不出现在 B 的档案中）；
- [ ] 断点续答可用（中途退出 → 再进入 → 回到原题）；
- [ ] 报告中的维度得分与逐题作答结果一致（抽样核对）；
- [ ] 导出的 PNG / HTML / JSON 文件可正常打开查看；
- [ ] DevTools 网络面板验证：运行期间无任何网络请求；
- [ ] 删除档案/清除全部数据有二次确认且确认后不可恢复的行为正确；
- [ ] 窄窗口下 7 个视图布局无溢出、无裁切。

---

## 11. 开放项（开发前/开发中确认）

1. **题量定稿**：各量表最终题目数量与具体条目，以选定量表的实际版本为准（开发时确认）；
2. **大五完整版 vs 精简版**：倾向完整版（40–60 题）以保证信度，若作答时长体验受影响再评估精简方案；
3. 多量表综合画像的呈现深度（v1 为基础并列版，后续版本再深化）。
