# 人格测试 App · 开发计划（Dev Plan）

> 文档版本：v1.0
> 编写日期：2026-09-21
> 依据：`prd.md`（v1.0）
> 状态：待用户确认后执行

---

## 1. 技术方案总览

| 项目 | 方案 | 决策理由 |
|---|---|---|
| 载体 | **单文件 Web 应用**（`index.html`，原生 HTML/CSS/JS） | 满足 PRD 硬约束：本地运行、无联网、刷新不丢；双击即可使用，无需安装任何环境 |
| 路由 | 页内 **hash 路由**切换 7 个视图（`#/`、`#/profile/:id` 等） | 单文件内多视图切换的标准做法，无框架依赖 |
| 存储 | **IndexedDB**（封装存储层 `db.js` 逻辑，失败可降级 localStorage） | PRD 指定；数据量为档案+逐题答案+报告，IndexedDB 结构化存储最合适 |
| 图表 | **Canvas 自绘**雷达图、得分条 | 零外部依赖，保证"无任何网络请求"；同时为 PNG 导出提供数据源 |
| 题库与文案 | 内置为 JS 常量（3 个量表的题目、维度、计分规则、解读与建议文案） | 无网络加载，完全离线 |
| 构建/依赖 | **无构建工具、无外部 CDN 依赖** | 双击 `index.html` 即运行 |

**关键约束**：最终产物必须满足——DevTools 网络面板全程无请求；刷新/关闭/重启数据不丢；`file://` 协议直接打开可用。

---

## 2. 项目结构

```
C:\Users\Lenovo\DoubaoWork\chats\2026-09-21\new-chat\
├── prd.md                        # 需求依据（已有）
├── dev-plan.md                   # 本计划（开发依据）
└── personality-test-app\         # 应用目录（M0 创建）
    └── index.html                # 唯一产物（单文件自包含：CSS/JS/题库/文案全内联）
```

---

## 3. 数据模型设计

### 3.1 IndexedDB Schema

| Store | KeyPath | 字段 | 说明 |
|---|---|---|---|
| `profiles` | `id`（自增） | `name`、`createdAt`、`updatedAt` | 档案（多人各自独立） |
| `attempts` | `id`（自增） | `profileId`（索引）、`scaleId`、`status: 'draft'\|'completed'`、`answers: [{qIndex, value}]`、`currentIndex`、`startedAt`、`completedAt`、`result: {scores, ...}` | 一次测试会话；draft=未完成（断点续答），completed=已出报告 |

### 3.2 题库结构（内置 JS 常量）

```
SCALES = {
  bigFive: { meta: {name, intro, itemCount, minutes, dimensions: [5维]},
             items: [{dimension, text, reverse}],
             interpretation: {dimension: {high: 文案, low: 文案}},
             advice: {dimension: {high: [建议], low: [建议]}} },
  resilience: { ... },   // 维度数以最终选定量表为准（单维总分用得分条呈现）
  cse: { ... }           // 核心自我评价，12 题
}
```

- 大五人格：**IPIP-NEO 50 题版**（5 维度 × 10 题，5 点同意度量表，含反向计分题）；
- 心理韧性：优先采用公共领域/明确开放使用的量表（开发时确定具体版本）；
- 核心自我评价：**CSES 12 题**（公开量表）。
- 全部题目开发时内置并逐条校对中文表述；交付说明中列出量表来源。

---

## 4. 里程碑与任务分解（顺序执行，每阶段可独立验收）

### M0 · 项目骨架与运行验证（约 1h）

| # | 任务 | 完成标准 |
|---|---|---|
| 0.1 | 创建 `personality-test-app/` 目录与 `index.html` 骨架 | 文件存在 |
| 0.2 | 实现基础视图容器 + hash 路由 + 重渲染函数 | 切换 hash 能显示对应视图 |
| 0.3 | 读取开发环境规范（Windows 兼容、视觉方向参考） | 无阻塞项 |
| 0.4 | 基础样式（柔和中性配色、正文≥14px、响应式降级） | 桌面/窄窗口均无溢出 |
| 0.5 | 浏览器打开验证骨架页 | 页面正常渲染 |

### M1 · 题库与数据层（约 3h，题库工作量大）

| # | 任务 | 完成标准 |
|---|---|---|
| 1.1 | 内置 3 个量表完整数据（题目、维度、反向计分标记、解读/建议文案） | 题数与量表规格一致，反向计分题已标注 |
| 1.2 | 实现存储层封装：`profiles` 增删改查、`attempts` 增查改 | 各操作在浏览器中可验证 |
| 1.3 | 存储降级策略（IndexedDB 不可用时提示并降级 localStorage） | 降级路径不报错 |

### M2 · 档案系统与首页（约 2h）

| # | 任务 | 完成标准 |
|---|---|---|
| 2.1 | 档案选择视图：空态引导、档案卡片列表、新建档案 | 空名称置灰；新建成功进入档案首页 |
| 2.2 | 档案首页：当前档案名、切换档案、测试中心（3 量表卡片+状态标签）、最近报告摘要、隐私提示 | 状态标签随测试动态更新 |
| 2.3 | 档案管理：重命名、删除（二次确认弹窗） | 删除提示"含 N 份报告、不可恢复" |

### M3 · 答题流程（约 3h）

| # | 任务 | 完成标准 |
|---|---|---|
| 3.1 | 量表介绍视图：说明、上次结果概要、开始/继续按钮 | 已测/未测/未完成三种状态分支正确 |
| 3.2 | 答题视图：进度条、一屏一题、5 点选项、上一题/下一题、未答置灰 | 交互规则与 PRD §7.2 一致 |
| 3.3 | 断点续答：每题作答立即入库；中途退出保存 currentIndex | 重进档案提示"未完成测试"，回到原题 |

### M4 · 结果报告（约 3h）

| # | 任务 | 完成标准 |
|---|---|---|
| 4.1 | 计分引擎：按维度聚合、处理反向题、标准化为 0–100 分 | 抽样手算 3 题核对一致 |
| 4.2 | Canvas 自绘雷达图（大五 5 维）与得分条（单维量表） | 数值与计分结果一致 |
| 4.3 | 报告视图：头部信息、雷达图、逐维度解读卡、综合小结、复测标注 | 文案随得分档位正确切换 |
| 4.4 | 个性化建议：按维度+高低分映射三类建议 | 建议与结果一致 |

### M5 · 历史与对比（约 2h）

| # | 任务 | 完成标准 |
|---|---|---|
| 5.1 | 历史报告列表（时间倒序）与单份详情复用 | 点击可查看任意历史报告 |
| 5.2 | 对比模式：勾选 ≥2 份同量表 → 叠加雷达图 + 维度升降标注 | 少于 2 份时按钮置灰 |

### M6 · 导出与设置（约 2h）

| # | 任务 | 完成标准 |
|---|---|---|
| 6.1 | 报告导出 PNG（Canvas → 下载） | 下载文件可正常打开 |
| 6.2 | 报告导出独立 HTML（内联模板，可离线查看） | 打开无外部依赖 |
| 6.3 | 全部数据导出 JSON 备份 | 可回读校验 |
| 6.4 | 设置视图：隐私说明、导出备份、删除档案、清除全部数据（均二次确认） | 破坏性操作全部有确认 |

### M7 · 自检、验收与交付（约 2h）

| # | 任务 | 完成标准 |
|---|---|---|
| 7.1 | 页面截图自检（桌面+窄窗口），修复视觉问题 | 无溢出、无错位 |
| 7.2 | 按 PRD §10 验收清单逐项核对（8 条） | 全部通过或逐条记录结果 |
| 7.3 | 兼容性验证：Chrome/Edge 打开、刷新/关闭/重启、`file://` 直开 | 数据不丢、无网络请求 |
| 7.4 | 交付 `index.html` 与使用说明 | 用户可直接打开使用 |

---

## 5. 测试与验收策略

1. **计分正确性**：每个维度随机抽 3 题，按公式手算对照应用输出；
2. **PRD 验收清单**：逐项执行 PRD §10 的 8 条（数据持久、多档案隔离、断点续答、得分一致性、导出可打开、无网络请求、二次确认、窄窗口无溢出）；
3. **兼容性**：Chrome 与 Edge 各开一轮全流程（建档案→测试→报告→导出→重启）；
4. **视觉自检**：截图核对桌面与窄窗口两种视口。

---

## 6. 风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| `file://` 下 IndexedDB 兼容性差异 | 部分浏览器可能限制 | 存储层降级 localStorage（数据量在 5MB 内）；验收时双浏览器验证 |
| 量表版权与翻译质量 | 合规与科学性 | 仅用公共/开放量表（IPIP、CSES），中文逐条校对，交付注明来源 |
| 单文件体积增长 | 可维护性下降 | 分段注释组织（题库/视图/逻辑分区），分批写入 |
| Canvas 导出 PNG 失败 | 导出功能不可用 | 无外部图片资源，不存在画布污染；失败时提示重试 |

---

## 7. 工作量估算

总计约 **18–20 小时**，按 M0→M7 顺序执行。每个里程碑完成即向用户汇报并验收，确认后进入下一个。

---

## 8. 交付物

- 最终产物：`personality-test-app/index.html`（单文件应用，双击即用）
- 使用说明：交付时附简短说明（打开方式、多档案用法、数据存储位置与隐私边界）
- 开发依据：`prd.md` + `dev-plan.md`
